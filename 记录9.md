# 记录9 — 交互日志

---

## 交互 1

### 我的提问（原文保留）

你是一个精通自然语言处理的python架构师。现在，我们要使用 Python 的 Streamlit 框架，结合 Hugging Face 的 transformers 库和 nltk 库，构建包含三种核心翻译体验的交互式应用。有两个文件你需要注意：

1. 记录9.md - 储存我们每一次交互我的提问（保留全部），你的回答及操作（这部分可以重点概括）
2. 代码写在week9.py里，依赖包放在 requirements.txt 里
接下来我会一步一步告诉你三个板块的内容。先不要对代码文件执行操作，把我当前的提问写入记录9.md。

### 助手回答与操作（概括）

- 角色与目标：按你的设定，以 NLP + Python 架构师身份协助，用 Streamlit + transformers + NLTK 搭建含三种翻译体验的交互应用；代码与依赖分别落在 `week9.py`、`requirements.txt`。
- 本轮操作：未修改 `week9.py` 与 `requirements.txt`；已将本条对话中的提问全文写入本文件，并建立「提问原文 + 助手概括」的日志格式，供后续交互追加。

---

## 交互 2

### 我的提问（原文保留）

第一板块：神经机器翻译模块（英译中）

功能需求：
1. Streamlit 应用的第一个标签页
2. 文本输入框 + "确认输入"按钮，允许用户输入英文句子
3. 使用 Hugging Face transformers 的 pipeline，加载 Helsinki-NLP/opus-mt-en-zh 轻量级模型
4. 输出模型生成的中文译文
5. 界面具有加载中的交互提示（Spinner）

### 助手回答与操作（概括）

- 在 `week9.py` 中实现：`st.tabs` 首页为「神经机器翻译（英译中）」；`st.text_area` +「确认输入」按钮；空输入时 `st.warning`。
- 使用 `@st.cache_resource` 缓存 `pipeline("translation", model="Helsinki-NLP/opus-mt-en-zh")`；点击确认后在 `st.spinner` 内完成取 pipeline 与推理，解析 `translation_text` 并展示中文译文。
- 第二、三标签页暂为占位提示，待后续板块补充。
- `requirements.txt` 写入：`streamlit`、`transformers`、`torch`、`sentencepiece`、`nltk`（为后续 NLTK 板块预留）。

---

## 交互 3

### 我的提问（原文保留）

我挂梯子执行了出现问题KeyError: "Unknown task translation, available tasks are ['any-to-any', 'audio-classification', 'automatic-speech-recognition', 'depth-estimation', 'document-question-answering', 'feature-extraction', 'fill-mask', 'image-classification', 'image-feature-extraction', 'image-segmentation', 'image-text-to-text', 'keypoint-matching', 'mask-generation', 'ner', 'object-detection', 'sentiment-analysis', 'table-question-answering', 'text-classification', 'text-generation', 'text-to-audio', 'text-to-speech', 'token-classification', 'video-classification', 'zero-shot-audio-classification', 'zero-shot-classification', 'zero-shot-image-classification', 'zero-shot-object-detection']"

File "D:\1university\研究生2\nlp\week8\.venv\Lib\site-packages\streamlit\runtime\scriptrunner\exec_code.py", line 129, in exec_func_with_error_handling
    result = func()
             ^^^^^^
File "D:\1university\研究生2\nlp\week8\.venv\Lib\site-packages\streamlit\runtime\scriptrunner\script_runner.py", line 689, in code_to_exec
    exec(code, module.__dict__)  # noqa: S102
    ^^^^^^^^^^^^^^^^^^^^^^^^^^^
File "D:\1university\研究生2\nlp\week8\week9.py", line 43, in <module>
    main()
File "D:\1university\研究生2\nlp\week8\week9.py", line 29, in main
    translator = load_en_zh_translator()
                 ^^^^^^^^^^^^^^^^^^^^^^^
File "D:\1university\研究生2\nlp\week8\.venv\Lib\site-packages\streamlit\runtime\caching\cache_utils.py", line 281, in __call__
    return self._get_or_create_cached_value(args, kwargs, spinner_message)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
File "D:\1university\研究生2\nlp\week8\.venv\Lib\site-packages\streamlit\runtime\caching\cache_utils.py", line 326, in _get_or_create_cached_value
    return self._handle_cache_miss(cache, value_key, func_args, func_kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
File "D:\1university\研究生2\nlp\week8\.venv\Lib\site-packages\streamlit\runtime\caching\cache_utils.py", line 385, in _handle_cache_miss
    computed_value = self._info.func(*func_args, **func_kwargs)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
File "D:\1university\研究生2\nlp\week8\week9.py", line 7, in load_en_zh_translator
    return pipeline("translation", model="Helsinki-NLP/opus-mt-en-zh")
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
File "D:\1university\研究生2\nlp\week8\.venv\Lib\site-packages\transformers\pipelines\__init__.py", line 943, in pipeline
    normalized_task, targeted_task, task_options = check_task(task)
                                                   ^^^^^^^^^^^^^^^^
File "D:\1university\研究生2\nlp\week8\.venv\Lib\site-packages\transformers\pipelines\__init__.py", line 355, in check_task
    return PIPELINE_REGISTRY.check_task(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
File "D:\1university\研究生2\nlp\week8\.venv\Lib\site-packages\transformers\pipelines\base.py", line 1338, in check_task
    raise KeyError(f"Unknown task {task}, available tasks are {self.get_supported_tasks()}")

### 助手回答与操作（概括）

- 原因：当前安装的 `transformers` 版本中，`pipeline` 已不再注册 `translation` 任务，故 `pipeline("translation", …)` 会触发 `KeyError`。
- 修改：第一板块改为 `AutoTokenizer` + `AutoModelForSeq2SeqLM` 加载 `Helsinki-NLP/opus-mt-en-zh`，用 `model.generate` 解码得到中文；`@st.cache_resource` 仍缓存 tokenizer / model / device；有 GPU 时自动 `cuda`。
- 功能与界面（标签页、输入框、确认按钮、Spinner、译文展示）保持不变；若曾缓存旧版 `load_en_zh_translator`，重启应用或清缓存一次即可。

---

## 交互 4

### 我的提问（原文保留）

我输入It rains cats and dogs 翻译得到的是下雨的猫和狗，你觉得这个错误是模型的限制吗，能否正确处理多义词和上下文语境？提供改进代码的方案但不用执行更改

### 助手回答与操作（概括）

- 解释：主要为小型 NMT 对**非字面、成语/习语**（compositionality 弱）的映射不足，易按词面译；非单纯「多义词」问题，上下文窗口对**单句习语**帮助也有限。
- 改进思路（仅方案，未改代码）：更强 MT 模型、双阶段（先释义/改写再译）、习语表约束、多语 LLM 提示翻译、后编辑与人工评估等。
- 操作：未修改 `week9.py` / `requirements.txt`；仅更新本记录。

---

## 交互 5

### 我的提问（原文保留）

模块一这样就可以了，好了，在第二个标签页，实现一个对比模块。 编写一个 Python 函数，模拟早期的『基于规则的机器翻译』：利用一个预定义的英汉词典字典（Dictionary），对输入的英文句子进行基于空格分词的『逐词直译』（如果词典中没有的词直接保留英文）。 将模块 1 中的 NMT 译文与这个『逐词直译』的结果在页面上并排展示，进行效果对比

### 助手回答与操作（概括）

- 第二标签页标题改为「对比：NMT 与词典直译」：独立输入框 +「生成对比」按钮。
- 新增模块级 `RBMT_EN_ZH` 与 `rule_based_literal_translate`：按空格分词，小写查表；未命中保留原词；对带首尾标点的词做简单剥离后查表再把标点拼回。
- 同句先算词典直译，再在 `st.spinner` 内调用与模块一相同的 `load_en_zh_translator` / `translate_en_zh`，用 `st.columns(2)` 左 NMT、右直译并排展示。

---

## 交互 6

### 我的提问（原文保留）

好的，在第三个标签页，实现机器翻译自动评测功能。 页面需要三个输入区域：1. 待翻译英文原文；2. 标准的中文参考译文（Reference）；3. 机器生成的候选译文（Candidate，可直接调用模块1生成）。 使用 nltk.translate.bleu_score 模块，计算机器译文与参考译文之间的 BLEU 分数。 在页面上输出最终的 BLEU 得分，并解释这个分数代表的意义。

### 助手回答与操作（概括）

- 第三标签页更名为「BLEU 自动评测」：三个 `st.text_area`（英文原文、参考译文、候选译文）；「使用模块 1 生成候选」在 `st.spinner` 内复用 `load_en_zh_translator` / `translate_en_zh`，将结果写入 `st.session_state.eval_cand_zh`（按钮位于候选框之前，保证同次运行内候选框能刷新）。
- 新增 `zh_tokens_for_bleu`（字级）与 `compute_sentence_bleu`：调用 `nltk.translate.bleu_score` 的 `sentence_bleu` + `SmoothingFunction().method1`；「计算 BLEU」输出 `st.metric`、百分制说明与 Markdown 释义（n-gram 重叠、非语义、字级与论文分词 BLEU 的差异等）。

---
